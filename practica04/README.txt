El programa se compila y corre desde "scr". (Compilar, Ejecutar)
"$ javac ./shapeSVG/Circulo.java"
"& java ./main/Main.java"

Con respecto a la ejecucion, así lo estuve ejecutando, pero me parece que eso es incorrecto. Mande un ejemplo de como ejecutar correctamente, por favor.

Para usar el main, descomente los métodos "escribir()", "todoEnSVG()" y "recursivo()", dependiendo de la prueba que quiera hacer. Puede hacer sus propias pruebas si lo desea, desde el main.

Ya se han puesto objetos estáticos por defecto en "main.java"

Para probar el html, ya se ha creado un archivo llamado "ejemplo.html", al cual puede enviarle lo que se corra en java. 
"$ java ./main/Main.java > ./main/ejemplo.html"


